version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: partyuser
      POSTGRES_PASSWORD: partypassword
      POSTGRES_DB: partyorder
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
